# girvel's NixOS configuration

- Developed on virtualbox, transition to real hardware planned
- Prefer to rely on dotfiles+stow instead of home manager (dotfiles are used on other linux distros), but home manager works too
